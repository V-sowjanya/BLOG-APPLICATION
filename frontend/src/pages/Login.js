import React,{useState}from'react';import axios from'../api/axios';import{useNavigate}from'react-router-dom';export default function Login(){const[formData,setFormData]=useState({username:'',password:''});const nav=useNavigate();return(<form onSubmit={async e=>{e.preventDefault();const r=await axios.post('login/',formData);localStorage.setItem('access',r.data.access);nav('/');}}><input onChange={e=>setFormData({...formData,username:e.target.value})}/><input type='password' onChange={e=>setFormData({...formData,password:e.target.value})}/><button>Login</button></form>);}